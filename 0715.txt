（共通）
GitBash起動
　cd /C/Users/mid/
  source .bashrc (password:password)
  cd Desktop/java-app
  git remote set-url origin git@github.com:ryoga404/java-app.git
  git pull origin main
  
  
TopPanel
　ユーザ登録ボタン→RegisterPanelへ
　ログインボタン→LoginPanelへ

LoginPanel
　パスワード入力時、パスワードは平文ではなく「・・・」形式になるように

HomePanel
　ログアウト処理（セッション削除処理）
　        JButton logoutButton = new JButton("ログアウト");

        logoutButton.addActionListener(e -> {
            int result = JOptionPane.showConfirmDialog(this, "ログアウトしますか？", "確認", JOptionPane.YES_NO_OPTION);
            if (result == JOptionPane.YES_OPTION) {
                mainFrame.logout();
            }
        });
　・現在「ログインしていません」となっているところをログイン中のユーザに
　JLabel userLabel = new JLabel("ログイン中: " + mainFrame.getCurrentUserId());　　（参考に）
　・データ登録ボタンで「AddRecordPanel」へ遷移するように
　・ログアウトボタンを実装



commit & push する前に必ずgit pull origin mainする
